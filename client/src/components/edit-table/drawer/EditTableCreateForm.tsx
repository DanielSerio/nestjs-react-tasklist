import type { EditTableCreateModalFormProps } from "./form.props";

export function EditTableCreateForm({
  endpoint,
}: EditTableCreateModalFormProps) {
  return <div>EditTableCreateForm {endpoint}</div>;
}
